<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<title>KURANISME</title>
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<style>
:root{
  --green:#2ecc71;
  --dark:#050505;
  --gray:#111;
}

*{box-sizing:border-box;margin:0;padding:0;font-family:Arial, Helvetica, sans-serif;}

body{
  background: radial-gradient(circle at top, #183018, #020202);
  color:white;
}

/* ===== HERO ===== */
.hero{
  min-height:100vh;
  display:flex;
  flex-direction:column;
  justify-content:center;
  align-items:center;
  text-align:center;
  padding:40px 20px;
}

.hero img{
  width:140px;
  margin-bottom:20px;
}

.hero h1{
  font-size:48px;
  letter-spacing:4px;
}

.hero p{
  margin:15px 0 30px;
  color:#9cff9c;
  letter-spacing:2px;
}

/* ===== BUTTONS ===== */
.btn{
  display:inline-block;
  background:linear-gradient(90deg,#1b3b1b,#2ecc71);
  color:black;
  text-decoration:none;
  padding:15px 40px;
  border-radius:40px;
  margin:10px;
  font-weight:bold;
  transition:.3s;
}

.btn:hover{
  transform:scale(1.1);
  box-shadow:0 0 25px var(--green);
}

/* ===== SECTION ===== */
.section{
  padding:80px 20px;
  max-width:1100px;
  margin:auto;
}

.title{
  font-size:32px;
  margin-bottom:20px;
  border-left:5px solid var(--green);
  padding-left:15px;
}

/* ===== GRID ===== */
.grid{
  display:grid;
  grid-template-columns:repeat(auto-fit,minmax(250px,1fr));
  gap:25px;
}

.card{
  background:var(--gray);
  padding:25px;
  border-radius:20px;
  border:1px solid #222;
}

.card h3{
  color:var(--green);
  margin-bottom:10px;
}

/* ===== FOOTER ===== */
